<html>
	<head>
		<title>Job</title>
	</head>
	<body>
		<center>
			<h1>Job: $job.getId()</h1>
			<table>
				<tr><td>Status:</td><td>$job.getStatus()</td></tr>
			</table>		
			#set($userjob=$job.getOwner().equals($currentuser))
			#if ($userjob && (!$job.hasStarted() || $job.isFinished()))
				<form method="POST" action="?method=delete">
					<input type="submit" value="Delete">
				</form> 
			#end
			
		</center>
	</body>
</html>