<plugin>
	<name>My Local Worker</name>
	<description><!-- add a description here --></description>
	<identifier>${groupId}</identifier>
	<version>${version}</version>
	<provider>${groupId}</provider>
	<repositories>
		<repository>
			<!-- add the url to the repository you have deployed your plugin to here, e.g. http://www.mygrid.org.uk/maven/repository -->
		</repository>
	</repositories>
	<profile>
		<artifact groupId="${groupId}" artifactId="${artifactId}"
			version="${version}" />
	</profile>
	<taverna>
		<version>1.6.0</version>
	</taverna>
</plugin>
